# Shared desktop overlay feasibility

## Current implementation checkpoint — September 9

Implementation continues locally after the accepted isolated native probe. S1-3 now includes the main-process host, private native session/preload, supervisor and utility-worker RPC/leases, persisted startup configuration, and bounded checksum-checked asset resolution. Audio and overlay privileged schemes are registered in the same pre-ready call because Electron permits that registration only once ([official protocol documentation](https://www.electronjs.org/docs/latest/api/protocol/)). Hardware acceleration policy and existing audio bundle names remain unchanged.

Fresh automated evidence: full Vitest run 203 files / 1,733 tests passed; all 33 Playwright browser workflows passed; all 19 Storybook suites / 202 stories passed with accessibility and console checks. Later focused regressions additionally cover canonical imported `sha256:` checksums (26 resolver tests), real runtime Twitch/Streamer.bot ingestion and imported image delivery without OBS (5 tests), and explicit rebind-away/back cancellation (19 sink tests). Full workspace typecheck and lint pass. Both web builds and the desktop preload bundler pass, and staging contains 139 runtime packages without source-tree links. These counts describe distinct runs, not one combined suite.

S1-3 through S1-5 source integration is implemented: private renderer/controller and staged React entry, first-class independent desktop recipients, protected Settings APIs, capability status, explicit per-surface drafts/save/retry, and live unified layer updates preserving media nodes and completion timers. Server and surface drafts share one navigation guard. New desktop work is blocked while a configuration change applies; disable/rebind invalidates pending media before changing the host, including rebind away and back. Imported asset hashes accept the runtime's canonical prefix and still verify exact contents.

Browser QA used regular Playwright because the Browser plugin/skill was unavailable. The flow was Settings -> select/enable desktop and edit independent unified visibility -> explicit saves -> reload retains settings. At desktop and 390px narrow width there was no horizontal overflow; targeted browser errors were absent. Screenshots are retained outside the repository under `C:\Users\James\.codex\visualizations\2026\09\07\01a07d40-9e32-76c2-a5be-28f4b6fda215\overlay-settings-live-ui`. UI guidance used Settings, Alerts module/browser-source, global feedback, accessibility and live-overlay error sections; this is the approved Windows follow-on, not marketplace/cloud/cross-platform or the Screen Effects module itself.

Validation classifications: Storybook initially hit sandbox filesystem denial resolving esbuild caches; the approved unsandboxed build passed. Lint initially included retained diagnostics and generated renderer bundles; generated-only exclusions now match source/build ownership, with production rules unchanged. The Storybook run passed despite duplicate-package warnings from retained probes; those paths are now excluded and discovery lists the same 19 story files without collisions. Existing desktop tests with forced termination on failure are not being run unchanged while BL-044 is open. No live profile migration/restart, commit, push or publication. Earlier “foundation only” and “startup unchanged” statements below are historical, not current source status.

### Packaged production-host regression — September 9, 23:38 EDT

`tests/desktop/overlay-host.spec.ts` passed against the freshly rebuilt unsigned package in 16.1 seconds (17.0-second Playwright run). It creates a separate config/data/Electron profile and loopback port, explicitly enables desktop output, selects a current display and shows the Alerts layer through real Settings. A saved enabled/reviewed Landscape document is resolved by the protected normalized-event test route without OBS clients. The actual custom-scheme renderer/preload displays silent text with sandbox/context isolation and no Node `require`. Hiding management leaves output alive; hide/re-show preserves a DOM sentinel and the original 12-second deadline; enabling a hidden occurrence does not replay it; disabling destroys the surface. This is real packaged service/host/renderer integration, not a replacement transport fixture.

Evidence: `C:\Users\James\AppData\Local\Temp\stream-jams-overlay-host-BrJi9N` retains configuration, process evidence and opt-in shutdown phases. Captured launch PID 170080 exited with code 0/no signal, and the test verified all ten captured PIDs absent (including actual Electron main PID 162572). Logs include service-stop, overlay-close-requested/overlay-closed and Quit. No forced cleanup or profile deletion. The prior run `stream-jams-overlay-host-YxgUDj` reached visible private text but failed a test-only assumption that Electron returns `preload` in its last-preferences snapshot; it also exited normally. The corrected check proves packaged app identity plus the live private bridge; independent archive inspection confirmed all three renderer resources and the bundled preload. Missing overlay phase names in the bounded logger allowlist were fixed with a failing/passing regression and rebuilt before the successful native run.

Remaining acceptance: representative video motion and physical input/focus/taskbar behavior in this integrated package, selected-monitor changes with real service ownership, OBS coexistence/alpha and independently routed audio. Earlier isolated media/display acceptance remains supporting evidence only. The full legacy desktop suite was not run because its failure cleanup can force-terminate processes; the slice remains incomplete until the declared remaining gates are resolved. No claim of BL-044 resolution or Screen Effects implementation.

Final source/package revalidation at 23:46 EDT: a late-constructor failure regression exposed a retained protocol handler when permission setup failed after protocol registration. Constructor failure now releases owned session/IPC/protocol resources as well as the native window; the failing regression then passed, with 31 focused private-host/log tests, root typecheck and lint passing. The package was rebuilt again and its main, private host and preload bytes compared against current compiled output; HTML references were confirmed relative. The unchanged native workflow passed again in 16.1 seconds (17.1-second run). Final evidence is `C:\Users\James\AppData\Local\Temp\stream-jams-overlay-host-YabNi6`: launcher PID 68544, actual main PID 101752, all ten captured PIDs absent, exit 0/no signal, archive SHA-256 `1db63cbdca5b14534bbdbdba597b6d82dc8529e5bf8ccc1ad5829134979fba8d`. Test profiles remain retained; the temporary Vite and Storybook servers were stopped normally. OpenSpec strict validation and whitespace checks pass; 17/20 tasks are checked, with acceptance/handoff tasks 5.1–5.3 still open. No native app remains open awaiting user interaction.

S1-1 gate status: **passed for the isolated native adapter/probe; proceed to S1-2** (September 9). Accepted disabled-acceleration runs cover 1080p/1440p motion, representative video/audio sync, input/focus, transparency/taskbar absence, three-monitor and mixed-DPI portrait placement, reported display removal without migration/replay, hidden controls, and bounded native Quit. No acceleration change or backend replacement is warranted. The earlier intermittent stutter has no confirmed cause and remains recorded below. This is not full-product acceptance: private transport, service loss/ownership expiry, recovery and actual Stream Jams service/tray/OBS integration remain in S1-3 through S1-6.

Entries below are chronological observations; historical statements that task 1.3 was pending describe that point in the investigation, not the current gate status.

## S1-2 persistence checkpoint — September 9

See also the partial S1-3 transport foundation below; no production host activation is implied by that work.

Implemented strict surface and visual-recipient identity contracts, complete-order validation, hidden-bottom registry reconciliation, and migration `020-overlay-surfaces`. The typed repository defaults desktop output disabled/unbound, retains saved rows across restart, converts existing unified registry paint order into topmost-first settings, and rolls back failed discovery/saves. The composition seam consumes independent unified rows without removing hidden modules' audio/TTS or changing module-specific outputs. Production runtime injection and isolated renderer stacking remain later integration work.

Backups retain envelope version 2. Schema-19 archives without surfaces have an explicit upgrade path to schema 20; older incompatible schemas remain rejected. Export and import clear desktop enablement/display binding while retaining opacity/layers. Local rollback points retain exact bindings, and failed replacement rolls back prior data. Strict configuration validation excludes renderer generation, credentials and other runtime fields.

Verification at this checkpoint: 14 focused Vitest files / 133 tests passed (core overlay modules and recipient contract, server surface repository, backup/database, native adapter). Core/server/desktop/desktop-test TypeScript project builds passed; scoped ESLint and `git diff --check` passed; strict validation of `add-shared-desktop-overlay-surface` passed. A test table argument-shape defect and database fixtures assuming migration 19 were corrected; no test was skipped or weakened. This is not a full workspace, Storybook, OBS or production lifecycle acceptance run.

No live profile was migrated and no existing service was restarted: production composition does not yet inject the new repository, and no Settings UI or host activation was added. No commit or publication was made. Native probe acceptance above remains supporting evidence, not proof of the future integrated transport.

Final checkpoint checks also passed: `node node_modules/typescript/bin/tsc -b tsconfig.json` (full workspace typecheck), scoped ESLint, whitespace and strict OpenSpec validation. One independent read-only GPT-6 Astra medium review found no significant checkpoint issues. Active-video node retention/current-offset behavior and stacking isolation remain S1-4 requirements; this optional composition seam is not yet injected into production playback.

### Partial S1-3 transport foundation — September 9

Added shared occurrence timing and strict visual-only batch/command/reply contracts, separate renderer/worker IPC envelopes, and a standalone `WorkerOverlayClient`. Tests cover stale generations/keys, two-second lease emission, disposal/send failure, bounded pending work, aggregate 128 MiB admission, cancellation during prepare, single start, completion deadline, expired-ready/start rejection, and overlapping stops. Media validation reuses existing per-kind size limits and requires exact referenced assets, matching kinds, unique IDs and dedicated exact-length non-shared buffers. It excludes audio/TTS and unexpected command/configuration fields.

One read-only GPT-6 Astra medium review identified backing-buffer over-transfer and overlapping-stop cleanup risks. Both received failing regressions and fixes: reject pooled/subview buffers before IPC, coalesce stop requests, and qualify occurrence cleanup by the captured record. Additional regressions caught early/double starts, aggregate-budget overflow and expired admission during completion grace. Final focused core-overlays/native-adapter/worker tests passed: 7 files, 50 tests. Full workspace typecheck, scoped ESLint, whitespace and strict OpenSpec validation passed.

This is a foundation checkpoint, not completed S1-3 or task 3.1: worker messages are not yet wired into the service/supervisor, and no main-process host, ownership-expiry enforcement, native sender/top-frame validation, preload, custom protocol, bounded asset resolver or renderer recovery has been added. No live service was restarted, no overlay was launched, and no commit/publication was made. Host-side enforcement must independently validate messages and budgets; a worker-side timeout is not proof that native visuals cleared.

Broader affected regression run also passed: `node node_modules/vitest/vitest.mjs run packages/core/src/overlays packages/core/src/overlay-modules apps/desktop/src apps/server/src/modules/overlay-surfaces apps/server/src/modules/backup apps/server/src/modules/db` — 28 files, 227 tests. This excludes packaged/native UI acceptance and the full repository test suite.

The early adapter is opt-in and selects an exact current display ID. It creates no window when disabled, unbound, or bound to an unavailable display. It requests a transparent, frameless, non-focusable, taskbar-hidden window with mouse pass-through, topmost placement and inactive showing after load. Display removal hides it; reconnect does not replay interrupted content. Display listeners are removed with the window. Renderer loss and load failure destroy it.

Normal Stream Jams startup is deliberately unchanged. The isolated probe copies the installed Electron distribution into test output and installs an owned minimal app containing the built adapter. This is packaged Electron adapter evidence, **not** final packaged Stream Jams service, tray or management acceptance. The automated neutral probe retains `app.disableHardwareAcceleration()` and plays no device audio. Later explicitly authorized representative-video sessions played default-output audio, as recorded below. No browser-source URLs were opened.

## Automated checks

- Policy and adapter unit regressions: nine tests passed after missing-module, inert-stub and pending-load race red runs. The race regression demonstrated a removed/reconnected monitor could show interrupted pending content; an interruption flag now prevents that.
- Strict workspace typecheck: passed.
- Neutral fixture recorder: generated silent 1920×1080 and 2560×1440 WebM clips, with actual reported MIME `video/webm;codecs=vp9,opus`. Recorder stops tracks, animation, timers and optional audio graph. Audio connects only to a media stream destination, never the device destination.
- First native attempt: failed at Electron launch readiness after inspector attachment. No media decode/native assertions ran. The first harness used top-level ESM `await app.whenReady()`; the working runtime registers a readiness continuation. The probe was corrected to that existing pattern. Earlier test-owned Electron and recorder process IDs were absent before the subsequent attempt. First-run evidence is retained in `test-results/overlay-window-packaged-na-986ed-ral-1080p-1440p-media-probe`.
- Second native attempt: still failed at Playwright `electron.launch` after both inspector and DevTools WebSockets connected. No native assertions ran. Evidence is retained in `test-results/overlay-probe-bootstrap`. Its owned PID 134916 was absent after failure. The readiness continuation improved the observed boundary but did not resolve launch, so the root cause is not established.
- Narrow standalone Electron characterization (no Playwright, recorder, overlay or product service): entry, ready, hidden window load and Quit all completed; native child exit code 0, elapsed 1031 ms, no stderr. This only establishes that minimal installed Electron can start and exit with acceleration disabled. It does not establish packaged overlay feasibility.
- Scoped lint and final strict typecheck passed. All affected desktop unit tests passed: 11 files, 58 tests.
- Narrow copied-app matrix: Playwright launched CJS in 720 ms but `electron.exe` reported unpackaged. Renaming only the copied executable to `Overlay Probe.exe` produced packaged=true and a passing probe (679 ms). Minimal ESM (678 ms), a blank partitioned window (683 ms), and the actual unloaded overlay adapter (658 ms) all passed with native cleanup. The earlier timeout cause remains unconfirmed; these results disprove a universal ESM/blank-partition attachment failure.
- Repaired full probe launched successfully but exposed a test defect: it assumed taskbar hiding required `WS_EX_TOOLWINDOW`. Electron 44.1.1 instead calls `ITaskbarList::DeleteTab`; the incorrect flag assertion was replaced by native no-activate/pass-through checks while physical taskbar absence stays pending. [Electron implementation](https://github.com/electron/electron/blob/v44.1.1/shell/browser/native_window_views.cc#L1165).
- Final native media probe passed (13.2 s including setup): packaged=true, non-focusable/topmost/initially hidden; native no-activate/pass-through styles present; hidden owned management-like window did not destroy the overlay; both clips completed; all captured native processes exited and isolated profile cleanup completed in 228 ms. Artifacts: `test-results/overlay-native-observations`.

| Recorded clip | Decoded corner alpha | Completed frames | Dropped frames | Completed media time |
| --- | --- | --- | --- | --- |
| 1920×1080 | 0 (transparent) | 60 | 5 | 1.978275 s |
| 2560×1440 | 0 (transparent) | 60 | 0 | 1.973203 s |

The 1080p observation dropped 5/60 frames. This is a retained smoothness concern and does not pass the physical smoothness gate. Transparent decoded pixels verify these specific fixtures retained alpha; physical desktop compositing remains a separate acceptance check.

### Reviewed controller rerun

The independent GPT-6 Astra medium review approved the scoped adapter/probe handoff with no blocking code findings, not overall native feasibility. Its minor recommendation is implemented: each decoded clip must now explicitly assert corner alpha 0, with an error identifying fixture transparency separately from native compositor support.

The controller ran `corepack.cmd pnpm exec playwright test --config playwright.desktop.config.ts overlay-window.spec.ts overlay-bootstrap.spec.ts --output test-results/overlay-controller-final`: **2 passed in 15.4 s**. Both clips again decoded alpha 0. The 1080p clip completed 61 frames with 3 dropped (1.966558 s); 1440p completed 60 frames with 0 dropped (1.969532 s). Confirmed owned native exit and profile cleanup took **477 ms**. This repeat does not erase the earlier 5/60 observation or certify visible smoothness. The runner emitted only an environment color-variable conflict warning (`NO_COLOR`/`FORCE_COLOR`); no assertion or cleanup failed.

Controller checks also passed: all 58 desktop unit tests, strict workspace typecheck, scoped lint, strict OpenSpec validation, and whitespace checks. Production startup remains unchanged, and no implementation commit or publication was made. OpenSpec tasks 1.1 and 1.2 are complete; 1.3 remains pending physical acceptance.

The harness records Win32 extended styles, current selected monitor geometry, decoded corner alpha and frame counters. A recorded opaque corner is a **recorder/fixture alpha limitation**, not proof that the native renderer cannot display transparent video. Counter observations are not a physical smoothness certification.

### Assisted on-screen session — September 8

The user explicitly approved and started a silent session using an isolated, temporary control window over the existing packaged adapter. They selected LG ULTRAGEAR, display ID `1811170625`, bounds `0,0 2560×1440`, scale factor 1. Selection did not start playback. After an explicit Start and countdown, the 1080p and 1440p clips each ran for 15 seconds and the overlay cleared.

The owned window recorded 3 button clicks and 8 input events during 1080p, then 25 button clicks and 56 input events during 1440p. The control reported keyboard focus at both clip starts and every recorded playback input sample. No typing content was logged. No automated input was injected during this interactive run; browser trusted-event flags alone are not treated as proof of a human action or full physical acceptance.

The user pressed Stop after both clips had cleared. At the 150-second session deadline, the app automatically closed; its Quit event was recorded 30 ms after that deadline. The launcher confirmed native exit code 0, no signal, and safe isolated-profile cleanup (151079 ms total launcher lifetime, not a Quit-duration measurement). Evidence is retained in `.superpowers/sdd/2026-09-08-shared-desktop-overlay-surface/assisted-runs/1788877813677-run/observations.jsonl` and `exit.json`. Only test profiles were removed after confirmed exit; test packages and observation logs remain.

User feedback confirmed typing and clicking worked, transparency was correct, and the desktop remained clearly visible. However, motion was very choppy: the square appeared to teleport across the screen rather than move smoothly. This fails visible-motion acceptance regardless of previously passing frame-count tests. The user saw a taskbar entry; whether it belonged to the expected control window or was an additional overlay entry is not yet established.

This confirms input and composited transparency on the selected scale-1 monitor, not overall native feasibility. Mixed-DPI/negative-coordinate operation, disconnect/reconnect and final product integration were not exercised. Task 1.3 remains unchecked. Diagnosis will first distinguish recorded clip content/cadence and its loop reset from native rendering behavior; the acceleration workaround remains unchanged.

### Recorded-motion diagnosis

A hidden, silent decode of the exact assisted-session assets recovered 60 unique timestamp/position samples for 1080p and 59 for 1440p. Median frame spacing was 33 ms, with median square steps of 30.06 and 40.74 asset pixels respectively (about 40 screen pixels for either clip on the selected monitor). No recovered new timestamp repeated the square position. The 1080p opening has an additional 98.75-pixel jump over its first 8 ms.

The manual harness looped these one-way sweeps. Measured resets were approximately 1849 asset pixels for 1080p and 2408 pixels for 1440p, about every 1.97 seconds. This establishes a nonseamless fixture defect that produces periodic teleporting independently of the desktop overlay. It does not establish that the fixture explains all user-observed choppiness or that the native compositor is smooth. Headless browser presentation is not a substitute for native compositor acceptance.

The diagnostic browser exited with code 0. No visible test or production fix was run during that diagnosis. Measurements, exact asset hashes and limitations are retained in `.superpowers/sdd/2026-09-08-shared-desktop-overlay-surface/recorded-motion-diagnosis.md` and its linked JSON files. The visible-motion gate remains failed until retested successfully.

### Single-pass comparison preparation and idle launch

The user subsequently thought the taskbar entry was probably the test control, but was not certain. Taskbar absence remains unconfirmed. They approved a corrected ordinary-window versus transparent-overlay comparison with controls hidden during overlay-only phases.

Two attempts to record a slower seamless fixture exposed recorder startup and endpoint timing limitations. The first failed cadence; a warmed recording removed the startup gap but still failed the unchanged loop-continuity check. Further loop tuning stopped, and the diagnostic narrowed to explicitly non-looping single passes. The reused 1080p and new 1440p assets both decoded alpha 0, median 33 ms/max 35 ms frame intervals, no repeated positions, and maximum steps of 6.50/6.35 pixels normalized to 1920-wide scale. Their loop jumps of 15.54/39.45 pixels still fail the 12-pixel criterion. They are not seamless assets or evidence of looping-media acceptance.

The scratch comparison plays identical bytes at matching full-display content bounds in ordinary and actual overlay windows, starting only after each window is shown. It records bounded timing/frame counters without visible-phase pixel readback. Controls are hidden and the reference destroyed during overlay-only phases. Each clip plays once (about four seconds), with an independent main-process seven-second clear deadline and a 150-second app deadline. Hardware acceleration remains disabled. The final hidden self-check passed phase order, explicit selection, matching 2560×1440 bounds, hide/restore, an intentionally unresolved renderer promise cleared by the main-process timer, and native exit/profile cleanup (8756 ms total, exit 0). Hidden stand-in windows and accelerated test playback are not physical acceptance evidence.

The controller launched the approved visible comparison, but no monitor selection or Start was recorded before the idle deadline. Consequently, no reference or overlay clip played. The app closed at its deadline, with confirmed native exit 0 and isolated-profile cleanup (150656 ms total launcher lifetime). The idle launch provides no new smoothness or taskbar evidence. Preserve `.superpowers/sdd/2026-09-08-shared-desktop-overlay-surface/comparison-readme.md`, `single-pass-media-current.json`, and `comparison-runs/1788879931157-run/` for the next explicitly started comparison. Only the temporary profile was removed after confirmed exit; media, package and logs remain. Production sources were unchanged during this diagnostic continuation.

### User-started single-pass comparison — September 8

After the user said they were ready, the controller reopened the unchanged comparison. The user selected LG ULTRAGEAR ID `1811170625` (2560×1440, scale 1), started the sequence, then explicitly repeated it. All eight phases reached the media end without a phase timeout. Every ordinary and actual-overlay window reported matching 2560×1440 bounds and content bounds. During all four overlay phases the control was hidden, the ordinary reference did not exist, and the overlay was visible. This confirms the test's isolation, not visual taskbar absence.

The ordinary reference recorded zero dropped frames in both resolutions on both runs. Overlay dropped-frame counters were 4/125 then 5/125 at 1080p, and 5/130 on both 1440p runs. Callback counts are recorded separately and are not interchangeable with dropped-video-frame counts. These measurements do not establish perceptual smoothness or its root cause.

Both sequences cleared normally. The user subsequently pressed Stop and Quit; the launcher confirmed native exit 0, no signal, and isolated-profile cleanup (100601 ms total launcher lifetime, not a Quit-duration measurement). Evidence is retained in `.superpowers/sdd/2026-09-08-shared-desktop-overlay-surface/comparison-runs/1788880842400-run/observations.jsonl` and `exit.json`. Only the temporary profile was removed; media, package and logs remain. No production code or rendering setting changed, and no native gate was marked complete.

The user then confirmed there was no additional taskbar entry for the overlay, only for the controls. Taskbar absence passes this assisted check. The ordinary reference was smoother but still looked below the expected 30 fps; the transparent overlay was very choppy, showing the square in only about six positions with jumps between them. This is a repeated visible-motion failure even with slow, non-looping, decoded-cadence-checked media. The user's estimated number of positions is not a measured display frame rate.

Controller analysis of the existing callback records, excluding the preloaded frame at mediaTime 0, found ordinary callback rates of 29.68–30.08/s and overlay rates of 26.76–29.04/s. The longest recorded callback interval was 54.6 ms in the ordinary path and 97.0 ms in the overlay path; no retained interval exceeded 100 ms. This does not account for only a handful of visible positions. The [requestVideoFrameCallback specification](https://wicg.github.io/video-rvfc/#dom-videoframecallbackmetadata-presentedframes) defines these counts as frames submitted for composition, not physical monitor scanout. The mismatch directs investigation toward presentation/composition after the measured media pipeline; it is not proof of a specific Electron, Windows, or driver defect.

Both paths use the app-wide hardware-acceleration disable call. A software-rendered transparent-window presentation problem is a leading, unconfirmed hypothesis. Do not simply remove this policy: `apps/desktop/src/main.ts` and [the existing shutdown evidence](windows-desktop-tray-runtime.md) document Windows 25H2 native main/GPU processes remaining after JavaScript Quit events, including a terminating GPU process that ordinary termination could not resolve. The proposed next experiment is an isolated copy of the same comparison with hardware acceleration permitted, verifying actual GPU feature status and observing both visible motion and native shutdown. This is a proposal only: no GPU setting, production code or new visible session changed after this feedback. Explicit approval is required for that known-risk experiment; a rendering improvement alone would not satisfy shutdown acceptance.

### Approved GPU-enabled experiment — idle session only

The user explicitly approved an isolated acceleration-enabled comparison after disclosure of the known native shutdown risk. Preparation changed only the staged acceleration policy and added experimental labeling, event-gated GPU status, and failure/owned-process evidence handling. The same media bytes, window policy, dimensions, single-pass sequence and timers were retained. The original comparison and production acceleration workaround remain unchanged. A controlled non-native regression failed against an identity transform and then passed for baseline-disable/variant-permit behavior, status readiness/timeout, and the existing phase/session deadlines. The controller independently reran that regression, checked syntax and staged asset hashes, and inspected failure handling before launch.

The approved launch is recorded in `.superpowers/sdd/2026-09-08-shared-desktop-overlay-surface/gpu-comparison-runs/1788882914329-run/`. After `gpu-info-update`, Electron reported `hardwareAccelerationEnabled: true`, `gpu_compositing: enabled` and `rasterization: enabled`. These are feature-status observations, not proof of a particular clip's hardware decoding or visible smoothness. No selection or Start occurred before the 150-second deadline, so no reference or transparent-overlay clip played.

The idle session exited normally: native exit 0, no signal, no forced cleanup, and 150794 ms total launcher lifetime. The controller independently found all six captured PIDs absent and the isolated profile removed after confirmed exit. Package, media and logs remain. This is one clean GPU-enabled control-window shutdown, not overlay-playback or repeated-shutdown acceptance. Motion is still failed from the prior user-started test, and the historical native shutdown risk is not disproved. No automatic relaunch or production change followed the idle session; the GPU variant is ready for a user-ready relaunch without further preparation.

### User-started GPU-enabled comparison — September 8

After the user confirmed readiness, the controller launched the unchanged isolated GPU variant. Its non-native regression, generated-runner/staged-main syntax and both source/staged media hashes had passed rechecking. The package-runner check encountered a sandbox cache-access error; running the dependency-free regression directly with Node passed. No existing Overlay Probe process was running before launch. Production hardware acceleration remains disabled.

The user selected a monitor and started one paired sequence. Electron reported `hardwareAccelerationEnabled: true` and `gpu_compositing: enabled` after `gpu-info-update`. All four phases ended normally at matching 2560×1440 content bounds. Controls were hidden throughout playback, and the ordinary reference was absent during both transparent-overlay phases. The staged media hashes matched the previously validated single-pass assets.

| Phase | Total video frames | Dropped video frames | Recorded callbacks |
| --- | --- | --- | --- |
| Ordinary reference, 1080p | 125 | 1 | 124 |
| Transparent overlay, 1080p | 125 | 0 | 125 |
| Ordinary reference, 1440p | 130 | 0 | 130 |
| Transparent overlay, 1440p | 130 | 0 | 130 |

The user pressed Quit after the sequence completed. The launcher confirmed native exit code 0, no signal and no forced cleanup; total launcher lifetime was 44509 ms, not a Quit-duration measurement. The controller independently confirmed all nine captured PIDs absent and the isolated profile removed. Package, media and logs remain in `.superpowers/sdd/2026-09-08-shared-desktop-overlay-surface/gpu-comparison-runs/1788890630929-run/`. No production process or diagnostic evidence was removed.

The user reported that the ordinary reference was fairly smooth with GPU acceleration. The transparent overlay improved but still jumped positions and was not smooth between jumps. Visible-motion acceptance therefore fails despite zero dropped overlay frames at both resolutions. The feedback applies to the comparison overall; separate per-resolution visual ratings were not supplied.

This is one completed GPU-enabled paired playback and clean native-exit run, not an acceptable overlay result. Permitting acceleration alone did not resolve the visible defect. The historical shutdown risk is not disproved, the production workaround is unchanged, and task 1.3 remains unchecked pending a scoped backend decision.

Code inspection also shows that the ordinary reference and actual overlay differ in focusability, mouse pass-through, topmost placement and session configuration, not just transparency. This comparison cannot attribute the remaining defect to transparency alone. The next diagnostic holds the actual overlay policy, GPU setting, media bytes and geometry constant while comparing opaque and transparent windows. It can distinguish a transparency-dependent difference from a problem shared by both overlay-policy variants, without claiming a specific native root cause. No production renderer policy or backend has been changed.

### Matched opaque/transparent preparation — no native launch

The user approved preparation of this diagnostic, not a visible launch. The scratch builder reuses the GPU-enabled launcher and the real built overlay adapter for both paths; the opaque copy overrides only the native transparency flag. Both load the identical HTML URL and media for each resolution and preserve pass-through, non-focusability, no-taskbar/topmost behavior, session, inactive showing and geometry. Opaque phases temporarily cover the chosen display. Controls are hidden in both paths, with no accessible Stop/Escape during playback; the existing phase/session deadlines and owned cleanup remain. Fixed opaque-then-transparent order does not rule out warm-up effects.

A non-native regression failed against the unchanged opaque placeholder, then passed for matched constructor settings and window calls, four-phase order, identical paired URLs, selection rejection, hidden/restored controls, an unresolved renderer cleared by the main timer, and the session deadline. The same regression passed against the actual staged adapter/main. The prior GPU regression, staged syntax, media hashes and whitespace checks passed. Preparation produced no native observations and no running Overlay Probe process.

The package is retained in `.superpowers/sdd/2026-09-08-shared-desktop-overlay-surface/matched-comparison-runs/1788891125488-prepare/`; operation, limitations and checks are recorded in `matched-comparison-readme.md` in the parent scratch directory. Production sources, prior comparisons and their evidence remain unchanged. No new physical motion or native shutdown result is claimed; the gate remains failed/incomplete pending the explicitly started comparison and scoped backend decision.

### User-started matched transparency comparison — September 8

After a fresh readiness confirmation and passing non-native regression, the controller launched the matched variant. No prior Overlay Probe process was running. The user explicitly selected a display and started one sequence; GPU status reported acceleration enabled and GPU compositing enabled. All four phases completed normally at identical 2560×1440 bounds/content bounds. Controls were hidden, no ordinary reference existed, and the matched player was visible in every phase. The launcher verified staged media hashes; both variants loaded the same page URL for each resolution.

| Phase | Total video frames | Dropped video frames | Recorded callbacks |
| --- | --- | --- | --- |
| Opaque overlay, 1080p | 125 | 1 | 124 |
| Transparent overlay, 1080p | 125 | 0 | 125 |
| Opaque overlay, 1440p | 130 | 0 | 130 |
| Transparent overlay, 1440p | 130 | 0 | 130 |

The user pressed Quit after playback. Native exit code was 0 with no signal or forced cleanup; total launcher lifetime was 37478 ms, not a Quit-duration measurement. The controller independently found all nine captured PIDs absent and the isolated profile removed. Package, media and logs are retained in `.superpowers/sdd/2026-09-08-shared-desktop-overlay-surface/matched-comparison-runs/1788891722265-run/`. No production setting changed and no diagnostic evidence was deleted.

The user confirmed that the opaque phases hid the desktop, validating the intended visible opaque/transparent contrast. Both variants seemed the same as the preceding transparent-overlay run, which improved with acceleration but still jumped and remained unsmooth between jumps. Both matched variants therefore fail motion acceptance. Removing native transparency alone did not improve the observed result; this points toward behavior shared by both overlay paths or another common factor, not proof of a specific defect. Separate per-resolution visual ratings were not supplied.

The user subsequently confirmed NVIDIA Background Application Max Frame Rate is OFF and not a factor. Do not revisit that setting or change it. Inactive/non-focusable showing also differs from the ordinary reference, but focusing on it before investigating the native presentation path was premature; no focus or production input-policy change is authorized.

The counters and clean exit do not establish acceptable physical presentation or resolve the historical native shutdown risk. No further launch or settings change was performed, and task 1.3 remains unchecked.

### Source-backed layered-window investigation and preparation — September 8

The pinned [Electron 44.1.1 Windows source](https://github.com/electron/electron/blob/v44.1.1/shell/browser/native_window_views.cc#L1284) adds both `WS_EX_TRANSPARENT` and `WS_EX_LAYERED` in `SetIgnoreMouseEvents(true)`. The real adapter calls this method. Both prior matched variants therefore retained the layered style despite one being visibly opaque: that comparison did not isolate layered versus non-layered behavior. This is a source-backed lead, not a confirmed performance cause.

[Microsoft documents](https://learn.microsoft.com/en-us/windows/win32/winmsg/window-features#layered-windows) click-through for layered windows using `WS_EX_TRANSPARENT`. Its [composition architecture discussion](https://learn.microsoft.com/en-us/archive/msdn-magazine/2014/june/windows-with-c-high-performance-window-layering-using-the-windows-composition-engine) distinguishes traditional layered surfaces from GPU composition; the historical description is not proof of the actual copies or bottleneck on this machine. DirectComposition/WebView2/shared-texture alternatives require a separately scoped prototype if warranted, not an immediate backend replacement.

The approved next diagnostic is staged under `.superpowers/sdd/2026-09-08-shared-desktop-overlay-surface/layered-comparison-runs/1788897998103-prepare/`. Both variants are opaque with identical media, geometry, focusability, topmost, partition and inactive-show settings. Only `setIgnoreMouseEvents(false)` versus `true` changes. The first and third phases intercept mouse clicks and all phases cover the selected display. Controls remain hidden during playback; the existing 7-second phase, 150-second app and owned 165-second launcher safeguards remain. Fixed order still leaves warm-up effects as a limitation. Native extended styles must be checked during the live run before interpreting the pair as a confirmed style comparison.

Preparation passed the real-adapter boundary regression (observed fail before implementation), staged four-phase/control-isolation/timeout checks, and staging media hash validation. Timestamp, renderer PID and native-handle evidence is emitted per phase. No native application or trace was launched during preparation. GPU permission remains scratch-only; production acceleration is unchanged, task 1.3 is incomplete, and physical motion remains failed.

An existing `C:\Program Files\NVIDIA Corporation\FrameViewSDK\bin\PresentMon_x64.exe` reports file version `1.9.12728.0` and has a valid NVIDIA Authenticode signature. Its own help confirms the planned switches. No tool download/install is needed. The generated `trace-command.json` uses a unique test executable/session name, 120-second capture, display tracking, date-time output, and disabled input tracking. It never replaces an existing ETW session and saves no ETL. ETW providers are system-wide even though CSV output is filtered to the unique test name. Only help/signature checks ran; tracing permissions and presentation-path visibility remain unverified. The [official console documentation](https://raw.githubusercontent.com/GameTechDev/PresentMon/main/README-ConsoleApplication.md) explains filtering, timing and presentation metrics; actual installed help governs compatible flags.

Readiness is required before another visible run. Start the separately bounded capture while controls are idle, verify capture health, then ask the user to Start. Correlate exact phase times, owned PIDs and swapchains; exclude controls/countdown/gaps from player summaries and retain dropped/not-displayed rows. Do not interpret absent rows as zero FPS, blend different swapchains into one FPS number, or treat renderer callbacks as display cadence. Unattributable or unsupported presentation data is inconclusive. Capture failure is not permission to enable broad WPR tracing, install a service, alter drivers or auto-elevate. Retain evidence; allow the capture to time out without stopping another session.

### Capture startup recovery — September 8

The first click-through comparison launch (`layered-comparison-runs/1788898563808-run`) lost launcher tracking but its controls remained alive; a non-elevated process-name check incorrectly suggested no process remained. The controller acknowledged the error after the user's correction. The first app later logged its automatic 150-second deadline and quit; an elevated exact-path inventory subsequently found no remaining diagnostic processes. The second launch (`1788898599670-run`) recorded user Quit and verified native exit 0 without forced cleanup. Neither played comparison clips or produced a usable capture. Do not count either as completed motion acceptance or automatically relaunch on an inconclusive process check.

NVIDIA's bundled PresentMon exited 1 without usable diagnostic output, including under a verified administrator token. Lack of privileges was therefore not established as its sole failure cause. After explicit user approval, the controller downloaded the official [PresentMon 2.5.1 standalone x64 executable](https://github.com/GameTechDev/PresentMon/releases/tag/v2.5.1) into the ignored diagnostic directory, without installing a service or changing permissions. SHA-256 matched the official release asset digest `9bec3083069f58f911e6a512f4806db51a27bd096103087bc1d05ef54c80a191`; Authenticode was valid with Intel Corporation as signer. Its three-second UAC capture preflight, filtered to a nonexistent diagnostic executable with input tracking disabled, completed with exit 0. This establishes successful capture startup/termination only, not observation of actual overlay presents. The next live capture must use this verified standalone executable, not the stale NVIDIA executable path in earlier generated trace plans. No overlay was launched during download/preflight recovery.

### Completed opaque click-through comparison — September 8

Run `layered-comparison-runs/1788900961386-run` completed all four clips normally. The user reported all four were equally smooth. Live `GetWindowLong(GWL_EXSTYLE)` checks against each logged native handle confirmed interactive phases had neither `WS_EX_LAYERED` nor `WS_EX_TRANSPARENT` (extended style 136315144), whereas click-through phases had both (136839464). All four had `WS_EX_NOACTIVATE`; each checked handle belonged to the owned browser PID 180356. Thus layered click-through alone did not reproduce the earlier defect in this opaque run. This does not establish transparent playback acceptance or identify why an earlier opaque run was choppy.

The user pressed Quit; the launcher verified native exit 0 without forced cleanup (106210 ms total launcher lifetime, not Quit latency). A separate exact-path process inventory found no remaining overlay processes. Official portable PresentMon PID 204880 ran the bounded capture under the approved UAC context and subsequently disappeared after its timeout; its exit code was not retained. `presents.csv` contains 363 records, all attributed to the owned GPU PID 25768 and `Composed: Flip`, across four swapchain addresses. Display intervals are available on 235 rows and display latency on 238. These counts are not an FPS measurement.

CSV wall-clock values around 12:57 correspond by apparent event timing to app UTC values around 20:57 (local machine time was 16:57). This eight-hour discrepancy has not been independently calibrated; do not silently correct it and report exact phase FPS. Even using that tentative offset, the last click-through phase lacks a confidently attributable player swapchain. Swapchain addresses also span more than one phase. Capture completeness/attribution is therefore insufficient for a four-way display-cadence comparison. Preserve raw data and use the physical feedback plus direct style checks for the current conclusion. The next scoped diagnostic repeats the already prepared opaque/transparent comparison; production policy and task 1.3 remain unchanged.

### Diagnostic controls visibility and deadline adjustment

Repeated matched-comparison attempts expired without playback while the user could not find the controls. One immediate native check reported visible, not minimized, not DWM-cloaked, at (855,366)-(1705,1026), in Windows session 1, matching Codex and Explorer. This did not prove user-visible placement or establish the original cause. No acceptance is inferred from those idle attempts.

With approval, the scratch matched builder now requests active `show()` and `focus()` on initial controls load only. Subsequent control restoration and overlay inactive showing remain unchanged. The 150-second playback deadline starts on the first valid Start (invalid selection and repeats do not reset it); an independent ten-minute absolute app limit and 615-second owned launcher watchdog remain. The launcher close wait is 610 seconds. No production source or renderer policy changed. The control page explains both limits. The TDD regression failed on missing active showing, then passed for active startup, no premature playback deadline, invalid selection, phase isolation, seven-second renderer timeout, playback deadline and absolute deadline. Tests also passed against staged artifacts, with syntax and media hash checks.

Changed live run `matched-comparison-runs/1788906350033-run` recorded controls visible and focused at startup, bounds (855,366), 850x660, with playback deadline unarmed. User confirmation of actual visibility is still required; no capture or playback was started by the controller at this point. Earlier staged packages retain their original settings and evidence. The inherited layered variant also receives these control/deadline changes if newly generated; its older readme/page timing description must be reconciled before another layered run.

### Successful matched transparent playback after controls adjustment

The user confirmed the changed controls were visible. Run `matched-comparison-runs/1788906350033-run` then completed opaque 1080p, transparent 1080p, opaque 1440p and transparent 1440p, each with `ended: true`, identical 2560x1440 bounds and hidden controls. The user reported every clip equally smooth. This is a positive GPU-enabled transparent-motion observation at both resolutions. Active showing was changed only for initial controls startup; this is not evidence that it caused the smooth motion, nor that the original defect is fixed. The media and overlay adapter settings were unchanged.

Quit was user-requested at app-relative 474338 ms, followed by the app quit event at 474354 ms. The launcher confirmed native exit code 0, no signal and no forced cleanup; its 474911 ms total lifetime is not Quit latency. An exact-path inventory found no remaining player processes. Capture used the verified standalone PresentMon with approved UAC, QPC timestamps, input tracking disabled and a 120-second deadline. Capture PID 191496 subsequently exited; its native exit code was not retained. CSV contains 576 `Composed: Flip` records across five swapchain addresses, all attributed to owned GPU PID 82632. Raw data is retained. No precise phase FPS or exhaustive display-cadence coverage is claimed: the app log lacks a directly sampled per-phase QPC timestamp, and the capture is supporting evidence rather than an override of physical feedback.

Task 1.3 remains unchecked because its production acceleration policy and remaining native acceptance requirements have not been satisfied. No backend replacement or production acceleration change is justified solely by this successful isolated run.

### Successful no-capture repeat

Run `matched-comparison-runs/1788908065818-run` repeated the same GPU-enabled matched comparison without launching PresentMon. Exact process inventory before launch found no existing diagnostic player or PresentMon process. All four phases ended normally and the user confirmed they remained equally smooth. The user requested Quit; the launcher verified native exit 0 with no signal or forced cleanup (215187 ms total lifetime, not Quit latency). The final exact-path inventory found no remaining player or PresentMon process. This is a second smooth matched transparent run, now without capture: PresentMon is not necessary for the currently observed smoothness. It does not explain the earlier failures, establish disabled-acceleration acceptance, or resolve the historical shutdown risk. The next discriminating comparison is the same probe under the production disabled-acceleration policy.

### Successful disabled-acceleration comparison

The user approved comparison against the production acceleration policy. A separate scratch staging wrapper reuses the matched runner and changes its staged main to call `app.disableHardwareAcceleration()` before readiness; it updates the diagnostic policy label and retains the same media hashes, adapter, four-phase sequence, active controls, inactive players and bounded deadlines. The policy regression rejected the GPU-enabled artifact, then passed against the software variant along with phase/isolation/timeout checks and generated syntax checks. No production file changed.

Run `software-comparison-runs/1788926081472-run` reported `hardwareAccelerationEnabled: false` after GPU status events. All four phases ended normally at matching 2560x1440 bounds with controls hidden. The user reported each clip equally smooth. No PresentMon capture ran. User Quit completed with native exit 0, no signal and no forced cleanup; launcher lifetime was 118606 ms, not Quit latency. The subsequent exact-path inventory found no remaining players. This supports transparent motion at both 1080p and 1440p under the existing disabled-acceleration policy. It does not establish that the original intermittent failure is understood or repaired. Retain that history and continue native display/lifecycle acceptance without enabling production GPU acceleration or replacing Electron.

### Three-monitor placement and qualified motion feedback — September 9

The user reports three monitors with equal Windows scaling. Run `software-comparison-runs/1788994814041-run` completed four phases on LG ULTRAGEAR, ID 1811170625, bounds (2560,0) 2560x1440, scale 1. It exited automatically on its playback-session deadline with native exit 0 and no forced cleanup (165692 ms total launcher lifetime). Its evidence was retained before the user-requested restart.

Run `software-comparison-runs/1788995217211-run` completed four phases on VG27A, ID 2589235279, bounds (0,0) 2560x1440, then four on portrait DELL U2312HM, ID 4022276724, bounds (-1080,0) 1080x1920. Both had scale 1, and each phase's logged window bounds matched its selected display. The run also exited automatically on the playback-session deadline, not user Quit; native exit 0, no forced cleanup, 347473 ms total launcher lifetime. Exact-path inventory found no remaining software-probe processes.

The user confirmed all tested monitors were equally smooth with no overlap or clipping, but explicitly qualified that desired smoothness is hard to judge without an example video. Placement, including negative-coordinate portrait placement, passes for this equal-scale setup. Neutral-fixture motion parity is observed, but representative-content motion-quality acceptance remains pending. Do not turn earlier descriptions of scoped smoothness into final product-quality acceptance. Next use a user-selected representative local effect/video; no asset has yet been selected or played, and no audio playback is authorized by this visual test feedback.

### Representative WebM with audio — September 9

The user selected `9855bf4d-ada5-4b68-952d-89dd21030f36.webm` from Downloads and explicitly requested audio. Muted headless inspection found 1920x1080, 6.417 seconds, with a transparent decoded first frame; this sample alone is not a whole-clip alpha analysis. The original 682941 bytes were copied without transcoding, SHA-256 `86d8aee0264dc8b6feef672b4b2b4b8bec6d198bc97dc77bd8928dc110d8ac07`. Original file unchanged; no upload. A separate scratch variant plays it once opaque and once transparent, audio enabled through the default output, acceleration disabled, each phase bounded at 12 seconds. The existing selection, controls and absolute/playback-session limits remain. Regression checks caught the old four-phase sequence, then passed for two phases, software policy and the new timeout; staged hash and syntax checks passed.

Run `user-video-runs/1788996952077-run` confirmed acceleration disabled and both phases ended normally at 2560x1440 bounds with controls hidden. The user reported both worked well and audio/video stayed in sync. This supplies positive representative 1080p-content motion and sync evidence on the selected display, supplementing the neutral fixtures; it does not imply coverage for all formats, frame rates, multi-device routing or OBS. User Quit completed with native exit 0, no signal and no forced cleanup (40206 ms total launcher lifetime, not Quit latency). Exact-path inventory found no remaining player processes. No capture ran and no production policy changed. Mixed-DPI and physical monitor-disconnect behavior remain unverified.

### Portrait fit and monitor power-cycle observation — September 9

The user explicitly requires aspect-preserving containment: resize to the limiting overlay dimension, never warp the content ratio. This matches the existing uniform-fit spec and staged `object-fit: contain`. A headless check of the staged original 1920x1080 clip in a 1080x1920 viewport confirmed centered contain layout, original intrinsic dimensions, no autoplay/loop, and audio enabled. Its expected contained image is 1080x607.5, with unused vertical space. The user confirmed correct proportions in the live portrait run.

Run `disconnect-video-runs/1788997332539-run` targeted DELL U2312HM ID 4022276724. Both opaque and transparent phases ended normally. The user power-cycled the monitor and observed no replay or migration to another screen. However, the instrumented app recorded zero `native-display-removed` and zero `native-display-added` events. Therefore this is a successful fit/power-cycle observation, NOT physical disconnect/reconnect acceptance: Windows/Electron did not report removal during this run. Disconnect safety still requires a real reported removal, using a cable disconnect or separately approved Windows display-disable action. At the initial post-feedback check the controls process remained alive and no Quit had been logged; no shutdown result is claimed here.

### Reported display removal through Windows Settings — September 9

Run `disconnect-video-runs/1789003336241-run` contains three explicit Starts targeting portrait DELL U2312HM ID 4022276724. In each attempt the opaque clip ended, then Windows/Electron reported that exact display removed during the transparent phase with a player present. The probe cleared the player and entered idle; each subsequent display-added event restored the same ID while idle with no player. No automatic replay is recorded. Removal-to-cleared log intervals were approximately 82, 83 and 196 ms; these are main-process log timings, not physical display latency.

The user reported audio cut off at disconnect and no content moved to another monitor across the repeats. This passes the isolated probe's logical display-removal/no-migration/no-replay behavior for this setup. It is a Windows Settings topology change, not evidence of a physical hot-unplug transport. The scratch host clears/destroys the media window on removal, so its in-video audio stopping is expected; this does not establish policy for independently routed production audio. It also does not independently prove the adapter's hide-only reconnect behavior because host cleanup destroys the adapter. That path retains unit coverage and requires appropriate integration verification.

The user pressed Quit, and native exit was confirmed as code 0 with no signal or forced cleanup (107521 ms launcher lifetime, not Quit latency). Exact-path inventory found no remaining probe processes. Three reconnect events restored the selected ID; no controller display-setting mutations were performed. Mixed-DPI validation remains pending.

### Accepted disconnect-audio behavior and mixed-DPI test authorization

The user explicitly accepted audio stopping when the desktop display disconnects and said independently routed audio is not required for this behavior. Do not require audio to survive that interruption. This scoped acceptance does not remove previously requested selectable audio-device destinations or change Alerts routing globally. The user also approved temporarily changing the portrait DELL from 100% to 125% scaling for mixed-DPI verification and restoring 100% afterward. Run `disconnect-video-runs/1789003663700-run` was launched for that user-operated test; its result is pending. No controller display-setting change or production implementation change was made.

### Mixed-DPI portrait playback — September 9

Run `disconnect-video-runs/1789003663700-run` selected DELL U2312HM ID 4022276724 at scaleFactor 1.25, logical bounds (-864,0) 864x1536. Both phases ended normally; content bounds matched the selected display exactly. The transparent window bounds also matched exactly. The opaque comparator reported outer bounds 866x1537 while its content remained 864x1536; do not describe its outer bounds as identical or infer a cause without further evidence. The user reported the test seemed fine after instructions to inspect proportions/clipping/spillover and restore 100% scaling. Restoration was not independently captured by the probe.

User Quit completed with native exit 0, no signal and no forced cleanup (52330 ms total launcher lifetime, not Quit latency). Exact-path inventory found no remaining probe processes. This supplies mixed-DPI portrait transparent-content placement evidence at 125%, alongside the previous 100% three-monitor checks. It does not establish every DPI combination, a live DPI change during playback, or final product integration acceptance. The user-facing scaling settings were not changed by the controller.

## Integrated representative-video attempt — September 10

Isolated profile `C:\Users\James\AppData\Local\Temp\stream-jams-integrated-video-ziwsru` launched packaged archive SHA-256 `1db63cbdca5b14534bbdbdba597b6d82dc8529e5bf8ccc1ad5829134979fba8d`, launcher PID 138304/main PID 167500. Controls used the approved original WebM unchanged and a silently decoded PCM soundtrack fixture through the existing device-audio route. Playback required a user-selected output and explicit Start; no OBS source was opened.

Initial enumeration at 04:08:53 UTC selected portrait DELL ID `4022276724`, bounds (-1080,0) 1080x1920, scale 1. Four user Starts returned queued, but each logged immediate `desktop-overlay.playback.failed`; no native overlay window appeared in the one-second samples. The user heard audio on the correct selected output (SFX / Elgato Virtual Audio), but saw no clip. This is audio-route acceptance only, not video or input-pass-through acceptance.

Read-only investigation confirmed the original imported video's metadata/checksum/bytes resolve correctly, and the actual saved editor document resolves and reaches a mock visual transport through the current compiled production sink. Live Settings then reported the saved DELL binding unavailable: the current DELL ID was `2316946718` at the same bounds/scale. The other two display IDs also differed from initial enumeration. The cause of this identity change is not established. Do not infer a media-decoder failure or silently substitute a monitor by label. Explicit re-selection of the currently enumerated DELL is required before retesting. At this checkpoint the test application remains open; no Quit or native-exit result is claimed.

### Automated integrated playback repeat — September 10

The first representative-video profile (`ziwsru`) later exited normally at 04:18:45 UTC with exit code 0 and all nine captured processes absent. The explicit-display-selection replacement (`zZsgjt`) remained idle overnight. Its first automation attempt failed before playback because the harness management session expired (HTTP 401), not because video failed. Normal Quit at 15:21:31 UTC confirmed exit 0 and all nine captured processes absent. The scratch controller now renews its session once after 401; production authentication was not changed.

Fresh profile `C:\Users\James\AppData\Local\Temp\stream-jams-integrated-video-i3eMg2` used the same packaged archive, explicit current DELL ID `2316946718` and the previously user-approved SFX (Elgato Virtual Audio) output. Playwright attached to the owned test app, selected both destinations, and exercised Start twice: controls visible, then management hidden via the controller. The Browser plugin was unavailable; repository Playwright was used. No OBS source was opened.

Both runs passed automated media checks: 1920x1080 decoded video in a 1080x1920 viewport, approximately 2 seconds of media-time advancement and 120 `requestVideoFrameCallback` callbacks per 2-second sample, no video error, and removal at completion. The fitted video rectangle was 1080x607.5 at (0,656.25), preserving 16:9 and centered in the portrait surface. Native samples showed the surface visible, non-focused and non-focusable at (-1080,0) 1080x1920. The separate audio element was playing, unmuted at volume 1, with an explicit sink and no media error. No observed page errors occurred. These are decoded-frame/timing and route-state checks, not proof of physical compositor frame rate or audible synchronization.

Retained report: `C:\Users\James\.codex\visualizations\2026\09\07\01a07d40-9e32-76c2-a5be-28f4b6fda215\integrated-video-automated-1789053778686.json`. Renderer screenshots `integrated-video-visible.png` and `integrated-video-hidden.png` were inspected; the captured instants were blank/transparent and a light fitted rectangle respectively, so they do not independently establish recognizable clip content or desktop compositing. Final perceived smoothness, audible sync and physical pass-through remain user-observed gates. Do not count synthetic browser clicks as physical pass-through acceptance.

Normal Quit completed at 15:22:58 UTC with exit code 0, no signal, and all ten captured processes absent (launcher 91796/main 181520). Shutdown evidence includes overlay teardown and Electron Quit phases. The application is closed, with evidence/profile retained and no forced cleanup. OBS coexistence and full acceptance remain outstanding; no production code or dependency changed for these checks.

## Native acceptance and closure reconciliation

| Check | Status / required evidence |
| --- | --- |
| Mouse and keyboard pass-through | Passed in the owned-window assisted check: user confirmed typing/clicking and the session recorded 28 clicks/64 input events. General game behavior is not inferred. |
| Focus preservation | Control focused at both clip starts and all input samples; user confirmed typing worked during playback. No unrelated application was activated for evidence. |
| Taskbar absence | Passed assisted comparison: user confirmed only the controls had an entry, with no additional overlay entry. Logs confirm controls hidden/reference absent during overlay-only phases. |
| Window transparency | Passed user-observed compositing: desktop clearly visible and transparency correct on the selected monitor. |
| Transparent moving video at 1080p and 1440p | Latest disabled-acceleration run PASSED user-observed smoothness at both resolutions, following two smooth GPU-enabled matched runs. Existing production acceleration policy can be retained. Earlier failed-run cause remains unresolved. |
| Mixed-DPI, negative-coordinate bounds | Equal-scale placement passed on all three monitors. Portrait DELL at 125% passed user-observed fit, with exact transparent window/content bounds at x=-864. Opaque comparator outer bounds differed slightly while content matched; see run details. Live DPI changes during active playback and other scaling combinations are not covered. |
| Selected-monitor unplug/changed identity | Three Windows Settings removals during active transparent playback passed probe clearing/no migration/no replay; same ID restored each time. Physical cable removal and changed-identity cases remain untested. Host cleanup masks the adapter's hide-only path; unit coverage is not final integration acceptance. |
| Management hide/background playback | Passed in the integrated desktop validation: playback continued with management hidden and no overlay taskbar entry. |
| Bounded Quit | Passed isolated and integrated native-exit checks without forced cleanup; the final routed-video manual stage also passed normal Quit behavior. |
| Audio and OBS coexistence | Passed through the integrated routed-video manual plan. Browser Source, selected physical-device, and desktop-overlay cases each had acceptable audio/visual synchronization. The accepted variable delay between independent device and OBS-monitoring paths is documented separately and is not shared-surface queue timing. |

The production transport, persistence, Settings UI, ordered layers, and integrated desktop/OBS paths are implemented and accepted. The September 12 repository gates passed lint, typecheck, build, Storybook, 1,832 Vitest tests, 34 Playwright tests, and 24 desktop tests with one intentionally skipped physical-output test. The user completed the remaining routed-video manual stages and accepted the documented independent-path audio variance. This closes the scoped shared-surface acceptance without claiming physical cable removal, every display/DPI combination, exclusive-full-screen support, automatic display-identity recovery, cross-platform support, or Screen Effects implementation.
